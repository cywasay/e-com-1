import PricingClient from "./PricingClient";

export const metadata = {
  title: "Pricing Engine | uniforms.ae",
  description: "Manage wholesale pricing rules and sales",
};

export default function PricingPage() {
  return <PricingClient />;
}
