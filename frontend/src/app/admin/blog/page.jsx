import BlogClient from "./BlogClient";

export const metadata = {
  title: "Blog Management | uniforms.ae",
  description: "Manage your store's blog content and announcements",
};

export default function BlogAdminPage() {
  return <BlogClient />;
}
