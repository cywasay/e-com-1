import RegisterClient from "./RegisterClient";

export const metadata = {
  title: "Create Account | uniforms.ae",
  description: "Join uniforms.ae to start ordering",
};

export default function RegisterPage() {
  return <RegisterClient />;
}
