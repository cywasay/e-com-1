import LoginClient from "./LoginClient";

export const metadata = {
  title: "Login | uniforms.ae",
  description: "Sign in to your uniforms.ae account",
};

export default function LoginPage() {
  return <LoginClient />;
}
